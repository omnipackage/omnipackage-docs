# Examples

Real working projects live in the [examples repo]({{ examples_github_url }}). Each has a ready-to-use `.omnipackage/config.yml` and a README with specifics.

## Language / build-system samples

- **C + Makefile** — the walkthrough used in [Getting started](getting_started.md)
- **Rust / Cargo**
- **Go**
- **Python**
- **Ruby**
- **Crystal**
- **Tauri** (desktop app)
- **Electron** (desktop app)

## Topic samples

- **Using secrets** — passing `${...}` values from `.env` into builds
- **Custom build scripts** — `before_build_script` hook

<!-- TODO: once any of these grow a non-obvious twist worth documenting inline, promote them to a dedicated guide page -->
