# `omnipackage release`

Convenience command that runs [`build`](build.md) followed by [`publish`](publish.md).

```
omnipackage release <project-dir> [flags]
```

Accepts the union of `build` and `publish` flags. See those pages for the full list.

<!-- TODO: when to use release vs. build + publish separately (CI staging?) -->
